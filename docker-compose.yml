services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: marcosfreitas-dev
    ports:
      - "127.0.0.1:8080:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
    restart: unless-stopped
